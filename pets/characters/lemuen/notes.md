# Lemuen 制作记录

- 角色：明日方舟「蕾缪安」
- 初始及默认姿态：`relax`
- 左键：播放一次 `interact`，结束后固定回到 `relax`
- 右键菜单：选择 `relax`、`sit`、`sleep` 或 `move`
- `move`：跟随鼠标；WASD 临时覆盖鼠标跟随；Shift 为 2 倍速

五个源文件均已按 SHA-256 逐字节校验复制，并已通过 Chromium 解码与透明渲染 QA。共享便携版成品位于 `../../packages/arkpets-desktop-pets/Arkpets Desktop Pets.exe`。
